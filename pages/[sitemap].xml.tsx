// This file is now obsolete and can be safely deleted.
// Sitemap generation is now handled by the API route at `pages/api/sitemap.ts`
// and routed via rewrites in `next.config.js`.

const ObsoleteSitemapPage = () => null;

export default ObsoleteSitemapPage;
